---
description: Design the AARRR funnel for Hardo App — activation aha, retention, referral loops, paywall placement
argument-hint: "[optional stage to focus, e.g. 'activation' or 'referral']"
---

Apply the `hardo-brand` and `gtm-strategist` skills.

Design Hardo App's funnel (Acquisition → Activation → Retention → Referral → Revenue).
Focus: **$ARGUMENTS** (default: full funnel).

Define the aha moment, the one lever per stage, drop-off hypotheses, the highest-ROI
fix, and which Supabase events to instrument (signup, mock_started, mock_completed,
feedback_viewed, paywall_hit, upgrade). Propose a North-Star Metric.
