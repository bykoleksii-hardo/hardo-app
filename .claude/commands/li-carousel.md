---
description: Build a LinkedIn carousel (document post) for Hardo App — slide-by-slide copy + visual direction
argument-hint: "[topic, e.g. '10 DCF questions' or 'behavioral questions that trip people up']"
---

Apply the `hardo-brand`, `linkedin-content`, and (for visuals) `design` / `ui-ux-pro-max` skills.

Create an 8-10 slide LinkedIn carousel for Hardo App on: **$ARGUMENTS**

For each slide provide:
- **Slide N** — the on-slide copy (big, minimal words, one idea).
- _Visual direction_ — a one-line note (layout, emphasis, palette cue).

Structure: Slide 1 = cover hook + payoff promise; middle = one idea per slide;
last slide = CTA (save / follow / run a free mock on Hardo).

Then add: a **caption** for the post (hook + 3-5 hashtags) and **first-comment** text
with the link placeholder. Technical accuracy mandatory; no fabricated claims.
