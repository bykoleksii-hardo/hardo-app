---
description: Build an X/Twitter thread for Hardo App — numbered, screenshot-worthy, CTA last
argument-hint: "[topic, e.g. '7 IB technicals that tank candidates']"
---

Apply the `hardo-brand` and `twitter-content` skills.

Create a 5-12 tweet thread for Hardo App on: **$ARGUMENTS**

Format: Tweet 1 = standalone hook + promise; tweets 2-N = one idea each (numbered,
tight, independently screenshot-worthy); penultimate = recap; final = CTA (follow +
free mock, link here). Technical accuracy mandatory; no fabricated claims. Also give
3 alternative hook tweets for Tweet 1.
