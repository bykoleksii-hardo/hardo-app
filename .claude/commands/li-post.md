---
description: Draft an on-brand LinkedIn post for Hardo App (3 hooks + body + hashtags + first comment)
argument-hint: "[topic or pillar, e.g. 'walk me through a DCF' or 'confidence']"
---

Apply the `hardo-brand` and `linkedin-content` skills.

Write a LinkedIn post for Hardo App about: **$ARGUMENTS**

If no topic is given, pick a high-value one from the Teach or Demystify pillar.

Deliver exactly:
1. **3 hook options** (first 2 lines each).
2. The **full post body**, formatted for the LinkedIn feed (short lines, scannable).
3. **3-5 niche hashtags**.
4. **First-comment text** (CTA + `[Hardo link]` placeholder + UTM suggestion).
5. One line: **why this works** (which pillar + why the hook lands).

Rules: technically accurate finance, no fabricated stats/quotes, no job guarantees,
rotate the CTA, link in first comment (not body).
